# TASKS — Phase 3, development

This is the live list. `TASKS-phase1.md` and `TASKS-phase2.md` are historical records. `DESIGN.md` defines the phase and the technical rules.

**Phase goal:** ARC becomes the harness used to write its own code and runs as an installed service. Success requires a week of real development through ARC and a full rebuild matching live state.

Bogdan assigns each task (`bogdan` or `claude`); the other reviews it. Statuses are `todo` → `in progress` → `in review` → `done`. Assignees below are intentionally unset.

Tasks are dependency-ordered; tasks at the same number may run in parallel. The phase has three parts: **substrate** (1–4: roles, registry, tools), **jobs** (5–6), and **production** (7: daily-driver operation).

**This phase is larger than Phase 2.** Once the substrate is usable, sections 1–5 satisfy the development half of the exit criterion. Move sections 6–7 to Phase 3.1 rather than expanding this phase.

## Decisions made before planning, 2026-08-22

- **Three configured roles:** concierge, executor, and archivist. Each is named for the work it does, not for where its model runs. There is no runtime difficulty classifier. `providers.md` records the current models.
- **The concrete stack to build against:** concierge = Gemini 3.7 Flash on a direct key; executor = DeepSeek V4 Pro via OpenCode Go's OpenAI-compatible endpoint; archivist = the existing Qwen3-8B sidecar. Budget target under $50/month against $100 today.
- **Roles use one model each.** Provider failure reaches the client. Fallback ladders need real outage or spend evidence before they become stateful policy.
- **Dispatch is a tool call with a delayed result.** `ToolCallIssued` starts the job. `ToolResultRecorded` carries the final summary. The existing unfinished-call recovery rule handles crashes.
- **Jobs run as supervised daemon tasks.** This keeps the conversation responsive; it is not containment. Process separation waits for a sandbox design.
- **The coding loop is generic:** send messages, run requested tools, append results, and stop. Planning and review are prompt or configuration policy until use proves they need machinery.
- **Sessions pin one provider for life.** This replaces v1's hot-swapping position because of cache economics.
- **Nothing prompts for permission.** What a project allows is configuration; a call outside it returns an error the model acts on. A per-call prompt would block the twenty-minute jobs it exists to guard. Phase 5 designs an actuator's confirmation against a real actuator instead of inheriting this.
- **This phase tests whether** a cheap model can handle most of about 19M monthly output tokens when the harness is strict. If it fails, investigate the harness first: strict `edit`, a working test loop, and rewind.

## Decisions, 2026-08-23

Taken after a read of pi, opencode, Claude Code, codex, and mini-swe-agent. Across those five the only universal core is a mutation tool plus a shell; everything above it is output shaping.

- **Four workspace tools: `read`, `write`, `edit`, `bash`.** `glob` and `grep` are cut. They are one search program with different arguments, that program is already on the machine, and DESIGN.md's rule already prefers the program that exists over a new builtin. Search moves to `bash`.
- **`edit` is string-replace, not a patch format.** Codex is the only harness on patch and it is OpenAI-only. No spike; follow the field.
- **Web is a builtin source, not a CLI.** It is the one capability a session needs before it can run anything, which is the exception the CLI rule already names. It keeps the shell out of the concierge, which is the session most exposed to untrusted text, and it keeps the search credential inside arcd. **Reversed on 2026-08-24: Gemini grounding does this better for the concierge and the executor has `bash`, so the tools were cut and only the source variant remains. See the note under section 4.**
- **Dispatch names a configured project.** A model selects from projects a human wrote; it never composes roots and modes. A voice session has no working directory because it is unbound and has no filesystem.
- **An actuator gets its own dispatch.** Starting a print or activating a system generation is confirmed at a turn boundary, not by a mid-turn prompt. This is prompt and configuration, not machinery.
- **A home-wide grant waits for the sandboxed worker.** OS-level change goes through a project over the Nix configuration instead, with `nixos-rebuild build` unprivileged inside the job and `switch` as its own dispatch.

## 1. Spikes (before schemas harden)

| # | Task | Assignee | Status |
|---|------|----------|--------|
| 1.1 | Test OpenCode Go and Gemini with the existing `provider/openai` parser. Confirm that Go's SSE output matches the Phase 1 fixtures. Decide whether Gemini's OpenAI-compatible API is sufficient or needs its own `Provider`. Deliver fixtures and a written decision. **Go passes unchanged; Gemini needs its own `Provider`, because a tool result cannot be fed back without echoing an opaque per-call `thought_signature` that has to survive replay.** | claude | done |
| 1.2 | Test both `consult_expert` modes. Run `claude -p` in a project with read-only tools and prove that read-only access is enforced. Measure a real plan request and a real diff review: latency, usable severity labels, and any usage signal in headers, exit status, or stderr. Deliver a decision and two argv templates. **Read-only is enforced on both `claude -p` and `codex exec`, and severity labels and usage come back structured. Keep these templates for a later expert-tool task; they are not part of the initial job path.** | claude | done |
| 1.3 | Test whether Flash chooses the right role, project, and brief. Score 20 scripted requests for wrong role, wrong project, and malformed brief. Use the result to decide whether dispatch needs a stronger model. **Flash is good enough — 0/20 wrong project and 0/20 bad briefs — provided every dispatch field is required with an explicit escape value, since optional fields get dropped.** | claude | done |

1.3 came back reliable, so dispatch stays on Flash. The findings that bind later tasks are folded into the rows below. Two remain unfiled:

- **Gemini caching is unverified.** No probe carried a prefix worth caching, so no cached tokens appeared in `usage`. Re-check with a real concierge prompt before trusting the 90% discount in `providers.md`. Explicit caching needs `extra_body.cached_content`, which the new `Provider` should carry from the start.
- **Gemini bills thinking it never streams.** There is no `reasoning_content`; one reply reported 70 completion tokens against 406 total. `Usage` under-reports output by about five times until the new `Provider` reads the right field.

## 2. Schemas (`arc-proto`)

Each schema change is its own commit, separate from code that uses it (invariant 3: additive only).

These are schemas and nothing writes a non-default value yet. 3.1 fills in the role, 5.1 the project, 5.2 the budget, and 3.3 the round-trip blob; the projection's `sessions.project` column stays NULL until then. The top-level `provider` and `model` keys are still the live path, and 3.1 replaces them with role resolution.

| # | Task | Assignee | Status |
|---|------|----------|--------|
| 2.1 | Record a session's role, project, and budget at creation so replay shows what a job ran with. Use the reserved model field on `ToolCallIssued`; do not add another copy. **`SessionCreated` fields 7, 8, 9: `SessionRole`, `project`, and an optional `Budget` of total tokens and wall-clock seconds. Absent budget means no budget.** | claude | done |
| 2.2 | `arc.toml`: `[roles.*]` and `[projects.*]`. A role resolves to one provider and model. A project resolves to its read-write root, any read-only grants, and its declared builtin/workspace sources. Do not add expert configuration, fallback policy, or workflow configuration. **The three role keys are fixed, so a typo is a load error rather than a silent extra role. Grants must be absolute — `~` is not expanded — and a read-only grant may not sit inside the read-write root, because a grant is a separate root and not a hole.** | claude | done |
| 2.3 | Add a generic opaque per-call provider blob to `ToolCallIssued`. Gemini rejects a tool result whose call did not carry back its `thought_signature`, about 620 bytes, and the transcript is rebuilt from the log, so it cannot be provider-local state. Name it for what it is — provider round-trip data — not for Gemini. Must land before 3.3 **Landed as `bytes provider_roundtrip = 9`.** | claude | done |

## 3. Roles and providers (`arc-core`)

| # | Task | Assignee | Status |
|---|------|----------|--------|
| 3.1 | Resolve each configured role to a provider and model. Add the role label to every `CompletionRequest` and trace span. This is the single place where Phase 3 work is measured. **Roles resolve once at startup and roles sharing an endpoint share one client. An unconfigured role falls back to the sidecar, so an empty config still runs. A role configured for gemini is a startup error until 3.3, rather than silently taking the OpenAI-compatible path that spike 1.1 proved wrong. The daemon-wide `provider` key is gone; `data/arc.toml` names the three roles instead, and the running release binary predates it.** | claude | done |
| 3.2 | Session pinning: role chosen at session or job creation, immutable for its lifetime. **An engine refuses to continue a session whose recorded role is not its own, before anything is appended; the client gets `role_mismatch`. The sessions table gained a `role` column, so the index rebuilds on next start. Sessions logged before roles exist carry UNSPECIFIED and stay unpinned.** | bogdan | done |
| 3.3 | Keyed providers: keys from `data/secrets/` (0700). `OpenAiCompat` sends no `Authorization` header at all today. Go needs nothing else — `glm-5.3` decoded every parser case unchanged, model ids are bare (`deepseek-v4-flash`, not `opencode-go/…`), and `/v1/models` exists so 3.4 can validate the allow-list at startup. Gemini gets its own `Provider`: it omits `index` on tool-call deltas, never sends `reasoning_content`, and needs 2.3's blob echoed back or the next turn is a 400 **Confirmed against the live endpoint, and two prose findings were wrong. `index` is missing on the tool-call object, not the choice, and the signature sits at `tool_calls[].extra_content.google.thought_signature` as base64 text that goes back verbatim — decoding it would only add a way to corrupt it. A plain text turn carries a signature too, but only function calls need one echoed: without it the reply is HTTP 400 `Function call is missing a thought_signature in functionCall parts`, not a degraded answer. Usage under-reports output because thinking is billed and never streamed, so output is `total - prompt` (measured 77 total against 10 prompt and 2 completion). Fixtures are in `arc-core/tests/fixtures/gemini_*.sse` and the live round trip is an `#[ignore]`d test. Go's base URL in `arc.toml` is `https://opencode.ai/zen/go`, one segment short of the published `/v1`, because arcd appends the version; config rejects the longer form rather than 404ing at the first turn.** | claude | done |
| 3.4 | Prefix stability for the concierge: identity file and record index render first and byte-identically, everything volatile after. A regression test asserts two consecutive turns produce an identical prefix. **The pieces were already deterministic — tools come out of a `BTreeMap`, the index out of `ORDER BY namespace, kind, title, id` — so this is the test that stops that being an accident, plus the ordering rule written where the prompt is built. It also asserts the prefix survives a restart, since the index is rebuilt by replay and that is where an ordering bug would surface. Two known prefix breaks are deliberate and stay: the final tool step drops the tool schemas to force an answer, and a consolidation pass mid-session rewrites the record index.** | claude | done |
| 3.5 | **Sidecar restart policy** (carried from Phase 1). It supports the archivist's consolidation work. **The monitor now respawns on an unexpected exit: three consecutive failures at 1s, 2s and 4s, then it stays down and says so rather than spinning, and a run longer than a minute counts as recovered so the count starts over. The device is re-resolved on every spawn, which is the same reason it is named rather than indexed. Verified against a fake sidecar — killed the child, watched the monitor bring a new one up — since a real one would have meant a second copy of the model on the card.** | claude | done |
| 3.6 | **Write `data/identity.md` for the concierge** — ARC's register per §5.1's four rules, plus the stable facts the always-loaded prompt should carry. Not code, and by invariant 7 not something an agent may write; this one is Bogdan's. Until it exists the concierge runs on whatever voice the model defaults to | bogdan | done |
| 3.7 | **Gemini on the native API.** `contents`/`parts`, camelCase, `x-goog-api-key`, `:streamGenerateContent?alt=sse`. **Done: the migration, not the grounding.** Four things the compat layer hid. `functionCall.args` is an object on the wire where the log stores a string, and `functionResponse.name` is required — resolved from the matching call rather than by widening `Message::ToolResult`. There is no `[DONE]`; `finishReason` ends the stream. There is no `NONE` thinking level either, so `Thinking::Off` and `Minimal` collapsed into one variant meaning *as little as this provider allows*. And `usageMetadata.thoughtsTokenCount` is reported, so output is exact instead of the `total - prompt` guess the compat layer forced. The parser lost its fragment accumulator too: native calls arrive whole. Verified live end to end, then the probe was deleted. **Grounding is not here.** It resolves through 4.1's `web` source and 4.1 is still todo, so wiring it now would mean a role-level flag that 4.1 would have to undo. Still outstanding for it: the `toolCall`/`toolResponse` parts, which need a proto change and have no place in the transcript; the attribution rendering; and the per-query billing. | claude | done |

Thinking is a per-role setting, not a global. `no_think` was a Qwen prompt hack sitting in front of every provider, and the sidecar's model alias was a top-level `model` key that meant nothing to a hosted role; both moved to where they apply, `[roles.*].thinking` and `[llama].model`. Each provider expresses the level its own way: Gemini sends `reasoning_effort`, the sidecar gets `/no_think` appended by the one code path that knows it is talking to the sidecar, and an `openai_compat` role rejects the setting at load because no endpoint there is measured to accept the field and an unknown field is a 400.

`minimal` is the level that stops thinking, and having it is a property of the model rather than of the API: 3.6, 3.5 and 3-flash-preview accept it, while 3.7 Flash rejects it identically on the native and OpenAI-compatible paths. The concierge therefore runs **3.6 Flash on `minimal`** — five runs of one chat turn gave 28–33 output tokens against 31–337 for 3.7 on `low`, and the live round-trip test ran in a fifth of the wall time. `low` is a cap the model may use rather than a level it obeys, so it is bimodal and unreadable from a single turn; an earlier three-run pass reported a clean ladder and did not survive more samples. Treat every thinking number as a distribution.

Nothing in config can catch `minimal` against a model that lacks it — it is a 400 at the first turn, not a load error. 3.4's `/v1/models` allow-list is the place to close that if it becomes a habit. `extra_body.google.thinking_config.thinking_level` reaches the same control and the two are mutually exclusive; `reasoning_effort` wins because it is flat and leaves the `extra_body.google` envelope free for the `cached_content` that caching still needs. Caching is still unverified: nothing has measured `cached_tokens` on a real concierge prefix.

## 4. Tool registry (`arc-core`)

| # | Task | Assignee | Status |
|---|------|----------|--------|
| 4.0 | **Split the engine from who runs a turn.** `Engine<P>` fused one log with one identity, so a job could not share the daemon's `Store` without either a second writer or the concierge's role pin — 5.3 was unbuildable as written. `Runner` now carries role, provider, model, thinking and the identity file, and arrives with each turn; `Engine` owns the log and the registry and has no type parameter. `AnyProvider` is gone with it: the trait returns a boxed future, so `Arc<dyn Provider>` works and adding a provider is adding a file rather than an enum arm. The sidecar's `/no_think` dialect became a `Sidecar` newtype instead of an enum variant, and `is_local()` went away — `replay` compares endpoints instead of asking a provider what it is. `Provider` now requires `Debug`, which keeps the key-redaction test honest across every implementation. No schema change, no proto change, no behaviour change | claude | done |
| 4.1 | Add a registry with builtin, web, and workspace sources. Declarations are session-scoped. Move the five memory tools without changing them. Do not add expert or MCP source plumbing. The `web` source stays a variant with no tools behind it: for a Gemini role it resolves to provider-side grounding, and it resolves to tools we write only if a role ever needs web off Gemini. Keep the variant, build nothing behind it. **A taxonomy change, not a new registry — dispatch, truncation and unknown-tool-is-an-error were already there.** A tool names its own source through the trait rather than being told one at registration, so the same tool cannot be offered under two sources and a new one cannot forget to say. `definitions()` now takes the source list; session.rs passes all three until 4.2 gives a session its own, which keeps this change behaviourless. The builtin tools moved to `tool/builtin/` and register as one list, so the daemon composes a source instead of seven tools and `tool/workspace/` is an obvious sibling for 4.3. `get_time` is builtin: DESIGN's five bullets never named it, but nothing else could hold it. `web` resolves to nothing and is tested as empty. Left for 4.2: arcd's config `ToolSource` still has only builtin and workspace, and it should stay that way — an unbound session is the one that needs the web and it has no project to declare it, so web resolves from the role, not from project config | claude | done |
| 4.2 | Session tool sources: a session gets the builtin and workspace sources its project declares, resolved once at creation and fixed for its lifetime. A call to a tool the session does not hold returns `ToolOutcome::ERROR` with the reason so the loop adapts. No runtime prompt and no durable verdict — there is no decision left to record. **Resolution re-derives per turn from two fixed inputs — the project name recorded in `SessionCreated` and a project map handed to the engine at startup — which is what "fixed for a lifetime" means once there is no durable verdict to store. Unbound means builtin only, so the live concierge lost the stopgap all-sources grant. A registered tool outside the session's sources is refused with its own message, distinct from an unknown tool, and never offered in the request. A project missing from config fails closed to builtin with a warning rather than killing the turn — grants list what is reachable, and a vanished entry grants nothing. The projection now stores `SessionCreated.project` (empty as NULL), the read 5.1 will also need. Nothing writes a project yet; the bound path is proven by seeded-log tests until 5.2's dispatch names one.** | claude | done |
| 4.3 | Workspace `read`, and the confinement it rests on. `resolve()` canonicalises every path and accepts it only under one of the session's granted roots, with `..`, symlinks, and absolute paths outside them rejected — tested adversarially. A grant carries a mode; `read` only ever needs the read one. The check lives in `resolve()` so every path-taking tool shares one gate. `read` caps and paginates, since it is what keeps a large file from spending the executor's context in one call. A rejected path returns `ToolOutcome::ERROR` with the reason so the loop adapts. **`tool/workspace/`: `Grants::resolve(path, Access)` is the gate. Paths must be absolute — a relative one is an instructive error, revisit if real sessions show friction. Existing paths canonicalise in full so a symlink at any component is resolved before containment; a nonexistent path canonicalises its parent and rejects a trailing `.`/`..`, which is what lets `write` resolve a file it is about to create. Containment is component-wise `starts_with`, so `/x/proj-evil` cannot ride a grant of `/x/proj`. Grant roots canonicalise at construction. Nineteen gate tests attack it. `read` pages (default 400 lines) with a continue-from marker and records a full-file hash per (session, path) — the anchor 4.4's staleness check consumes. arcd registers none of this until 5.1 gives sessions grants. One note for 5.1: overlapping grants resolve first-match, order-dependent — config's no-nested-grants rule is what makes that unreachable, keep it.** | claude | done |
| 4.4 | Add workspace `write` and `edit`. `edit` is string-replace. Both refuse a path whose grant is read-only, so a session can read notes it cannot change. `edit` must match exactly one occurrence and reject a file changed since the last read — that rule is why `read` stays a tool, since a file read through `cat` gives it nothing to compare against. Test this thoroughly. **One staleness rule shared by both tools, with distinct refusals the loop can act on: never-read says read it first, changed-underneath says read it again, and `edit` names the actual occurrence count when the match is ambiguous. A new file writes without a prior read — nothing to be stale against — but an overwrite needs one. A successful write or edit re-records its own content hash, so a tool's own change never trips the next edit; hashes stay per-session, and a read under another session id does not count. `old` must be non-empty and differ from `new`. 23 tests across the two tools. The gate needed nothing — 4.3's parent-resolution rule already covered creating a file that does not exist yet.** | claude | done |
| 4.5 | Add `bash` with a scrubbed environment. It is now the search and listing path, which makes two things load-bearing: a deterministic output cap with a timeout, and a `PATH` in the scrubbed environment that carries the search tool, git, and the toolchain. Scrubbed is not empty — Nix and cargo also need `HOME`, `USER`, and the `XDG_*` vars, or builds fail in ways that read as bugs. `bash` runs as the user with nothing between it and the filesystem; the grants are a tool-level check, not containment. A sandbox is later work. **The pass-through is an allowlist — `PATH`, `HOME`, `USER`, `TMPDIR`, `LANG`, `XDG_*` — never a denylist, and this settles §3.1's open redaction question: a result cannot carry a credential the process never received. Runs `--noprofile --norc` in the read-write root, stdin closed (spike 1.2: CLIs block on an open one), 16 KiB per stream cut at a char boundary, 120s default timeout clamped to 1–600. The child gets its own process group and a timeout SIGKILLs the group, so a dead build cannot leak grandchildren. One failure found in review and fixed with a regression test: a background child holding the pipes past bash's exit hung the call — and the engine turn with it — forever; the drains now get a 500ms grace and then the group is killed, which delivers EOF and keeps the captured bytes. `ripgrep` joined the devshell in this commit, since routing search through `bash` makes it a dependency. Reply shape is deterministic: bare stdout on a clean quiet exit, `exit {code}`/stderr sections only when present, no durations.** | claude | done |
| 4.7 | **Token budget re-measure** (carried, re-scoped). The Phase 2 note assumed a 16k local concierge; with a hosted one the ceiling changes and the question becomes which sources load into which session. Measure before 5.2 wires anything else always-on | — | todo |

The two expert invocations from spike 1.2 are retained here for the deferred expert-tool task. Both were verified read-only against a workspace on 2026-08-23. Close stdin: both CLIs block on an open one. The prompt is a positional argument. claude has no `--cd`, so set the child's working directory; codex takes `-C`. codex runs commands through `zsh -lc`, a login shell that sources the user's profile, so set `shell_environment_policy.inherit` explicitly for invariant 8.

```
claude -p "<prompt>" --model opus --fallback-model sonnet
  --tools "Read,Glob,Grep" --strict-mcp-config --setting-sources ""
  --permission-mode manual --no-session-persistence
  --json-schema '<schema>' --output-format json < /dev/null
```

```
codex exec "<prompt>" -s read-only --json --ephemeral
  -C <project root> --output-schema <schema file> -o <result file> < /dev/null
```

`--strict-mcp-config --setting-sources ""` is load-bearing. Without them the child inherits the user's MCP servers — a plain `claude -p` came back holding Gmail, Calendar, and Drive tools, none of which are read-only or inside the workspace. codex cannot be closed the same way: `--ignore-user-config` leaves `web__run` and its bundled app tools in place, so its read-only covers the filesystem and not the tool surface. Prefer claude when a diff may contain secrets. `--bare` would cut claude's prompt prefix but reads auth only from `ANTHROPIC_API_KEY`, so it cannot run on the subscription.

On the web source: measured on 2026-08-24, Gemini's Search and Maps grounding do not work through the OpenAI-compatible endpoint — `google_search` is an invalid tool type there and `extra_body.google` has no `tools` field — but both work on the native API on 3.6, compose with `minimal` thinking, and combine with our own function tools once `toolConfig.includeServerSideToolInvocations` is set. In one round trip the model searched, circulated the result, and called our function with an argument derived from it. That is a better answer for the concierge than tools we write: no search credential to hold, no unbounded page text to cap, and citations included. The executor keeps `bash`, and DESIGN's rule already prefers the program that exists over a new builtin. So the tools are cut and only 4.1's source variant survives.

The price is that the concierge becomes pinned to Gemini for a capability rather than for cost and latency. Swapping it costs a feature from here, not a config line. Accepted deliberately on 2026-08-24.

On 4.5: `rg` is on erebor but not in `flake.nix`'s devshell. Routing search through `bash` makes that a dependency rather than a convenience, so it goes in the devshell in the same change as the `PATH` the tool hands its children.

On changing the machine: a project over `~/dotfiles` plus one sudoers entry for `nixos-rebuild switch` reaches the whole system without a home-wide grant, and every change is a reviewable diff with a generation to roll back to. Two rules make it hold. The rule that grants the power cannot live where the power reaches, so the sudoers entry and arcd's own unit and `arc.toml` stay outside any granted root — otherwise one rebuild widens the grant to `ALL`. And `switch` is a separate dispatch, because a mid-uptime switch leaves the running kernel on the old generation and the mismatch surfaces at the next reboot. `build` and `--dry-activate` need no privilege at all, so the job iterates freely and only activation needs a yes.

On 4.3 and 5.1: the workspace is a set of granted roots, not one root. Settled in DESIGN.md under workspaces and confinement, and in invariant 8, so the tasks below implement a decision rather than making one.

The reason to grant rather than deny is the same as principle 6 in `providers.md`. A deny list fails open the first time an entry is forgotten. A grant list fails closed: `~/.local/state/arc/` is unreachable because nobody granted it, not because it was banned. Do not redact tool results either — redaction is unbounded pattern-matching and it lies to the model about what it read. DESIGN.md already settles the adjacent case for `bash` by scrubbing the environment, which is the same argument: protection comes from what the tool can reach, not from a regex afterwards.

The residual risk is worth stating rather than designing around. arcd runs as the user, so the process can read those keys whatever `resolve()` says. Confinement is a check in a tool, not an OS boundary, and the file-read path has no backstop equivalent to the scrubbed environment. Closing that needs privilege separation, which is not this phase.

## 5. Jobs

| # | Task | Assignee | Status |
|---|------|----------|--------|
| 5.1 | Workspace binding: `sessions.project` plus a set of granted roots on disk, and the rule that unbound sessions get no workspace tools. The project root is granted read-write; anything else the session should reach — notes, dotfiles, a reference checkout — is a separate read-only grant. Grants are a list of what is reachable, never a list of what is forbidden, so a path nobody granted is unreachable by construction rather than by remembering to ban it. Grants are session-scoped and belong in the log, since replay has to rebuild what a tool call was allowed to see. **`WorkspaceGrant` rode in its own schema commit. Grants canonicalise once, at `Engine::create_bound_session` — a missing root fails at creation, not mid-job — and the recorded roots are the authority from then on: a session continues under its logged grants even when config has changed since, tested explicitly. At turn time they flow projection → `TurnContext` → the gate; `Workspace` kept only the staleness map and the tools read grants from the call context, with a distinct no-workspace error if one arrives grantless. `resolve()` still canonicalises every request, so a root that drifts to a symlink after recording fails closed. Projection schema bumped to 8 for the `session_grants` table. arcd now registers the four workspace tools; they stay unoffered in unbound sessions by 4.2's source filter. The TUI conversation stays unbound — only 5.2's dispatch creates bound sessions.** | claude | done |
| 5.2 | Add the dispatch tool. It creates a child session with its own role, tool sources, and budget, then returns the summary later. The project is named from the configured set, never composed by the model; with none named it lands in a standing scratch project and the handback says where it went. It uses the existing tool-call recovery model. Every field is required with an explicit escape value in its enum — 1.3 measured optional fields being silently dropped, which was most of the local model's errors. The concierge prompt must say that recall is answered directly and that `archivist` is for extraction. **The tool validates and the engine acts, mirroring how memory events already flow: `ToolReply` gained `job_request`, and `tool_step` creates the child and rewrites the ack to name its session id. Every field is required — role and project are enums, `"none"` resolves to the configured `scratch` project and errors instructively when none exists, `0` budgets mean unbudgeted. The child records the dispatched role, the project's grants, and the budget; its `SessionCreated` carries `Source::Model`, since the session exists because a model asked. One known imprecision, corrected by 5.3: the child row records the concierge runner's provider/model, because the engine does not know other roles' runners until the supervised task owns the child `Runner`. The 3.2 pin keys on role alone, verified by test — an executor runner continues the child. The ack says plainly that the job has not started; execution arrives with 5.3. The concierge-prompt line about recall vs extraction lives in the tool description; any `identity.md` reinforcement is Bogdan's by invariant 7.** | claude | done |
| 5.3 | Supervised job task: owns the generic loop, talks to the same `Store`, and leaves the daemon responsive. It is not a sandbox and does not add process supervision or restart semantics. **Landed as two commits, because the row's own criterion demanded it: the engine held one mutex across a whole turn, so a job running a build would have frozen the conversation. First the engine went `&self` — store behind a short-scope sync mutex never held across an await, model streaming and tools unlocked, and a per-session guard so same-session turns serialize while sessions interleave; consolidation's re-check-then-append kept its atomicity in one lock scope. Then the supervisor: `Reply.jobs` carries each dispatched child, arcd spawns one task per job with the role's own runner (identity file stays concierge-only), the brief becomes the child's first user message so its transcript stands alone, and shutdown drains jobs on the daemon's existing grace. `create_bound_session` now records the child role's provider/model from role identities set at startup, closing 5.2's noted imprecision. A failed job logs and dies; nothing restarts it. Found in passing, pre-existing: the `TraceCapture` tests race on `set_default` under parallel test threads, ~1/100 flake on the unmodified baseline — parked with 7.4's leftovers.** | claude | done |
| 5.4 | Steering: messages to a running child, queued and processed in order **A steer is a user message to the child: the server routes it to the supervisor before the engine ever sees it, the job queues it, and each steered message runs as an ordinary child turn after the current one — order preserved by the queue, durability by the same send_message path every turn uses. The completion race is closed under one lock: a job removes itself from the live map only after re-checking the queue under the lock steers send under, so a message either lands and runs or the sender learns the job is done and the client gets the honest role_mismatch fall-through. A failed turn drops queued steers with a counted warning rather than running work after an error. The steering client gets Accepted and a bare StreamEnd; the steered output lands in the child log, which is where 6.1 will surface it.** | claude | done |
| 5.5 | Budget enforcement per job, in tokens and wall-clock, recorded at dispatch and checked by the task **Checked at turn boundaries only, by design: cancelling a turn mid-await can leave a durable `ToolCallIssued` with no result until the next restart's orphan repair, so a running turn always finishes and `MAX_TOOL_STEPS` bounds the overshoot. Tokens are input plus output summed across the brief turn and every steer; a zero field means that dimension is unlimited. One check sits at the top of the steer loop — after the last turn and before the next at once. Over budget reuses the failed-turn finish: live entry out, queued steers dropped with a counted warning, dimension and spent-vs-allowed named in the log. Nothing durable is written here — the outcome record is 5.6's handback.** | claude | done |
| 5.6 | Handback: a summary plus the child's session id into the parent, with the full transcript staying in the archive **Landed per the amended §4.1: the dispatch ack stays, and the handback is its own `MessageAppended` — `Source::System` for honest attribution, wire role `user` because the transcript builder only feeds user and assistant rows to the provider, so a system role would be silently invisible to the model. It takes the parent's turn guard, so a summary never interleaves inside a running turn, and the job leaves the live map first, so a steer racing the handback falls through honestly. All three endings hand back: clean with the child's final reply, "stopped: the turn failed", and "stopped: token/time budget exhausted" naming spent-vs-allowed; no reply becomes "(the job produced no reply)"; summaries cap at 2 KiB with the marker. The wire's `HistoryEntry` carries no source, so the TUI currently styles a handback like a user message — distinguishing system rows in the client is 6.x polish. First live run, 2026-08-26: the summary quoted raw DSML tool-call markup the executor leaked into its final prose — summary quality is a prompt or archivist concern, not a handback one; no markup-stripping.** | claude | done |

## 6. TUI

Expanded on 2026-08-26, deliberately overriding the move-to-3.1 guidance above: the first live day showed the daemon working and the TUI hiding it. The goal of this list is a working agent, and for a daily driver the ambient layer is not polish. 6.3 → 6.4 is the sequence; 6.5–6.11 are independent and slot between larger tasks.

| # | Task | Assignee | Status |
|---|------|----------|--------|
| 6.1 | TUI: refreshable job list showing live jobs, status, and budget consumed. The conversation stays usable while one runs. **`:jobs` mirrors `:review` in architecture and look: one elided row per job — state, role/project, spent-vs-budget tokens, elapsed, id tail — running in plain, terminal dimmed, selection accented; `r` refetches, nothing polls. Job state is live daemon memory rebuilt empty on restart; the durable record is the child session. Terminal entries are retained to a cap of 50, oldest evicted, running jobs always listed first. The conversation staying usable while a job runs was already true from 5.3's concurrency work; this makes it visible.** | claude | done |
| 6.2 | **Session titling pass** (carried). Now cheap: titles are `archivist` work and the job list needs something better than a session id to display. **Titling rides the consolidation pass — no new scheduler: before extraction, an idle, untitled session with a real first exchange gets a sidecar title, at most six plain words, sanitized hard, appended `Source::System` as `SessionTitled` (latest wins on replay, so retitling is appending). `commit_title` keeps the pass's discipline with its own lock scope and two re-checks: the session grew, or another pass titled it, and the title is discarded whole. No projection version bump — the column predates the event and nothing ever wrote one, so there is no stale index to force a replay over. Picker rows already preferred titles; job rows now substitute the title for role/project when one exists.** | claude | done |
| 6.3 | Server push: a `Subscribe` request whose response stream never ends. The server pushes small notification frames — job state changed, message appended to a session — correlated to the subscription's request id, fitting the existing framing. Notifications carry facts, not payloads; the client refetches what it cares about. Supervisor and engine get a broadcast hook; connections forward. Schema is its own commit **One broadcast channel from the daemon; engine and supervisor send, subscribed connections forward. The no-interleave guarantee is structural: the connection loop was already strictly sequential per reply, so the notification arm of its select cannot fire mid-stream — proven by a gated-turn test asserting frame contiguity. Notifications carry the fresh `JobInfo` (title filled via the same lookup the list uses) or a bare session id; the client queues frames arriving while it awaits a different reply. Lagged receivers warn and continue. Known gap, accepted: consolidation and titling append through the store directly, not `Engine::record`, so those two event kinds push no notification — background archival is poll-visible, and wiring them can ride any later task that needs it.** | claude | done |
| 6.4 | The ambient layer on 6.3: a one-line status strip where the rule line lives — jobs running, spent tokens, elapsed — always current, elapsed ticking locally. A handback landing in the open conversation appears in it without a keypress. `:jobs` live-updates while open; `r` stays as a manual nudge **The net loop went event-driven: on connect the client subscribes, and the loop selects between commands and a cancel-safe `next_notification` — pushed frames never sit unread in the socket again. The strip shows running jobs only, newest first — a finished job's evidence is its handback in the transcript, not a lingering line — and renders in the rule line, which is untouched when nothing runs. Elapsed ticks from a 1s interval gated on a running job existing: a clock display, not a freshness poll. Handbacks appear live: a `session_appended` for the open session refetches history once per burst via an in-flight flag, and is suppressed while our own turn streams, since deltas already paint it. `:jobs` rows patch in place by session id while open.** | claude | done |
| 6.5 | `HistoryMessage` carries the event source (additive field, own schema commit) so a handback renders as system output — dim, unmistakably not the user's words — instead of styled as something Bogdan typed **The source now flows event → a new `messages.source` column (projection version 9, forced rebuild — this one is a DDL change, unlike 6.2) → `MessageRow` → the wire → a `system` block in the transcript: dim header, dim body, unmistakably not the user's words. The wire role stays `user` so the model keeps seeing handbacks; only the display tells the deeper truth. Pre-source rows read back as unspecified and render as before.** | claude | done |
| 6.6 | Tool lines say what they did: elide the call's arguments to width — `bash python main.py · ok`, `write main.py · error`. The data is already in history; watching a child session becomes watching. Also stop rendering an empty assistant bubble for a text-less tool turn **Arguments plumb end to end — engine event, wire frame, client, block — and history renders identically to live, so a reopened child session reads the same as watching it. The summary is the first non-empty string value in the arguments object, which covers bash's command and the path of every workspace tool without per-tool cases; unparseable arguments fall back to the bare name. A text-less tool turn no longer renders a lonely `arc` header. One caveat noted for the future: serde_json iterates keys alphabetically, invisible for today's single-string-arg tools.** | claude | done |
| 6.7 | A per-turn cost line, dim, after each reply: tokens in and out and latency. The $50/month bet should be felt daily, not discovered in 7.3's accounting **A `Block::Cost` in the transcript, so it scrolls with the turn it prices: tokens compact (`2.3k`), seconds one decimal, dim. Pushed only for a timed turn with nonzero usage — faults keep their fault line, steer acks stay silent. Latency is measured client-side from submit to stream end.** | claude | done |
| 6.8 | Steering from the jobs popup: `Enter` on a row opens the child session, `s` opens an input line that steers it. The mechanism landed in 5.4; this is the reach **`Enter` reuses the picker's exact open path; `s` borrows the real input editor under an `s> ` prompt with the conversation's draft stashed and restored. Steering is just a send to the child's session — 5.4's routing does the rest — but the agent review caught that the turn's `Accepted` frame would have reassigned the open conversation, so a pending-steer guard makes those two frames no-ops; safe because the net loop serializes one send at a time. A steer to a job that finished meanwhile surfaces the plain role_mismatch fault, no special case. Footer confirms `steered {id-tail}` until the next key.** | claude | done |
| 6.9 | Picker QoL: `/` filters sessions as you type; times render relative. Compounds with 6.2's titles **`/` reuses the steer-prompt mechanism — the query lives in the input bar with a `/` prefix, wrapping and cursor for free; `j`/`k` are filter characters while typing, arrows navigate, `Esc` restores list and draft whole. Times compact to `now/{n}m/{n}h/{n}d/{n}w`, the week tier replacing the absolute-date fallback.** | claude | done |
| 6.10 | `Ctrl-j` inserts a newline in the input, so a multi-step brief is written as one **The spec assumed the input renderer already walked lines — it did not; the wrap ignored `\n` entirely. Rewritten as one pass building rows and cursor position together, preserving the old boundary rule so height allocation stays right. `Ctrl-j` guards on insert mode with no picker open, since `Ctrl-p` can raise the picker from insert. `submit` trims only the ends; interior newlines survive to the wire.** | claude | done |
| 6.11 | `:help`: one dim popup listing the keys. The surface has grown enough that discoverability is real **Built from a const key table read off the actual handlers, so the card and reality change in adjacent hunks. Found in passing: `:sessions` never existed — the picker is `s` or `Ctrl-p`; the card documents what is. Overflow truncates with a dim ellipsis line rather than growing a scroll system.** | claude | done |
| 6.12 | Popup legibility: shared chrome for picker, review, jobs, and help — a plain ASCII border with the popup's name in it, a cleared margin so the transcript never bleeds into the panel, consistent padding. One isolated commit touching rendering only, so it reverts clean if the bare look was better **One `popup()` helper: `+---+` ASCII border, dim, with the popup's name in the top rule; all four panels (sessions, review, jobs, help) converted onto it, their in-body title lines and hand-rolled geometry deleted. Content rendering untouched. Rendering-only as promised — reverting this commit restores the bare look exactly. Border and title accent orange after first live use: dim chrome was invisible against a busy transcript.** | claude | done |
| 6.13 | `Ctrl-o` returns to the previously open session — opening a job from `:jobs` currently strands you there with no way back but the picker **`Ctrl-t`, the tag-jump-back idiom (`Ctrl-o` was taken by the thought toggle). Swap semantics rather than a stack: it bounces between the last two sessions, which is exactly the job-and-back flow. Guarded while streaming; in the help card.** | claude | done |
| 6.14 | Serve a turn with the runner of the session's own recorded role, not always the concierge's. Typing into a finished job session becomes a legitimate follow-up conversation with the executor in its workspace — today it is a raw `role_mismatch` fault. A live job's session keeps routing through the steer queue first. Manual turns on a job session run outside its recorded budget; accepted and noted **The server resolves the runner from the session's recorded role through the same role map the supervisor already owns — no second construction, no engine change, the pin passes naturally. Steer-first routing for live jobs is untouched and now proven to never reach the engine. The identity file stays concierge-only. An unmapped role falls through to the old honest fault. The test that asserted the old role_mismatch behavior was replaced by one asserting the conversation works.** | claude | done |
| 6.15 | The handback turn: when a job's summary lands in a concierge parent, the daemon drives one model turn over it, so the concierge tells the user what the job achieved — and can dispatch again if the result demands it. Fifty consecutive handback turns without a user message is the backstop, not the governor; near-simultaneous handbacks collapse into one turn; narration is the turn's own short reply, taught by prompt. Amends §4.1's letter and narrows §3.1's "arcd does not re-drive the model" to what it always meant: crash recovery. Without this the handback is a message nobody reads aloud **`Engine::continue_session` drives a turn with no user message, sharing `send_message`'s loop through one `drive_turn` — pin, sources and grants still resolve per entry point so a pin failure appends nothing. The supervisor runs the turn after every handback on a concierge parent; a handback landing mid-turn marks the parent dirty and earns one catch-up turn, so bursts cost at most two reactions. The autonomy counter lives with the supervisor, resets on any user send, and at fifty skips the turn with a warning — the summary itself is already durable. A handback turn that dispatches spawns its children through the same path as a user turn, which is what makes chains real. Found in review, deferred: a job's own turn still discards `Reply.jobs`, so a grandchild is created durably but never run — fixed in the next commit.** | claude | done |
| 6.16 | `continue_job(session_id, message)`: the concierge speaks downward. A live job queues it as a steer; a finished one resumes its loop with the message as the next turn — same role, same provider, warm cache, whole workspace state intact. Without it the concierge's only reaction to "done, but I assumed X" is a fresh child with amnesia **The `job_request` pattern verbatim: the tool validates shape, the engine validates identity (distinct errors for a session that does not exist and one that is not a job), and the supervisor knows liveness — steer if running, respawn over the same session if finished, with the check-and-insert under one lock so a raced resume cannot clobber a steer channel. `parent_session` is whoever's turn asked, so the handback comes home to a concierge conversation, a handback turn, or another job alike. The end-to-end chain test runs: handback turn continues a finished job, the resumed job hands back, another concierge turn follows. A resumed job shows running again in the list with fresh token counts — noted, acceptable.** | claude | done |
| 6.17 | Jobs always end and the list tells the truth. A watchdog forces `failed` for a task that dies without a terminal state (a panic marks nothing today); a silence timeout on job model calls (10 min without a frame fails the turn — reqwest streams have no read timeout, so a stalled SSE holds a job open forever) routing through the normal failed-handback path; and terminal entries age out of `:jobs` after 10 minutes — the ending is already narrated in-conversation and the child session is the durable record. Chosen over stay-until-50 after a finished job haunted the list all evening **The watchdog wraps the job task and awaits its handle — a panic anywhere in the job's code lands as `failed` with a "the job crashed" handback, so the concierge narrates even crashes. The silence rule: 600s without an engine event fails the turn, any event resets the clock, and a pending tool call suspends it entirely — bash bounds itself at 600s, so nothing hides. The dropped turn future leaves the exact partial shape the startup orphan repair already covers; no new machinery. Terminal entries age out of the list after 600s with the 50-cap kept as backstop. Found in review and fixed in the same commit: the handle vector grew unboundedly for the daemon's lifetime — finished wrappers are now reaped at each spawn.** | claude | done |
| 6.18 | Projects carry a one-line human-written description in config that flows into the dispatch tool's schema, so the concierge grounds "your implementation repo" to `arc` instead of guessing from bare names. First live failure: an analysis job sent to empty `scratch`, elaborately analyzing a deleted Conway game. Second drive: 0/3 — three dispatches, including a vague brief, all grounded to `arc`; the row stands on the one incident **`[projects.*].description`, optional and empty by default, rendered into the `project` property's description ("arc: ARC's own implementation repo. …") since JSON Schema enums cannot describe their values. The live `data/arc.toml` carries the two descriptions.** | claude | done |
| 6.19 | Harden `continue_job`'s description: for changing course or adding work, never for requesting output — a finished job's reply is already in its handback. First live incident, diagnosed from the log: seven pokes in one reaction turn's tool loop — the model expected the tool result to be the answer, got "Continuing job X.", and retried. The supervisor routed the burst perfectly: first call resumed the job, six queued as ordered steers, no duplicates (the raced-resume guard), one handback at the end. So the fix is words in two places: the ack must say the reply arrives later as a handback, and the description must forbid calling again to fetch it. The deeper fix is identity.md orchestrator guidance, which is Bogdan's by invariant 7. Second drive, 2026-08-27, 3/3 reproduction and the diagnosis widened three ways. The pokes fire in the dispatch turn itself, before the job has produced anything. They are not just clutter: a "Status update?" steer made the executor redo its whole analysis with tools — 53k input tokens for that one steered turn, ~441k for a job whose brief cost 41k — so the pokes multiply job cost by several times. And the model never chooses `continue_job` for its real purpose: told "have the same review job also check X", it dispatched a fresh child and poked that instead, so the warm-context resume 6.16 built goes unused. The description has to draw both lines: never call it to fetch output, do call it instead of `dispatch` when the work belongs to an existing job **Words in three places, no machinery. The description draws both lines and names the price (each message costs the job a full turn). Both continue acks — engine and tool-side — say the reply arrives later as a handback and forbid calling again to fetch it. The dispatch ack, where every storm started, now ends "End this reply and wait." Whether words hold against a model that mistook a tool result for an answer three times in three cycles is the next drive's question; identity.md guidance rides 6.21.** | claude | done |
| 6.20 | Yank to system clipboard via OSC 52: a key that copies the last reply or a picked message whole — terminal selection can't reach app-managed scrollback and drags popup chrome along **`y` in normal mode copies the last arc reply, partial included; the app returns a `Command::Yank` payload and main.rs writes `\x1b]52;c;<base64>\x07` at the terminal edge, so the app layer stays testable and nothing fights ratatui's buffer. Footer says `yanked`/`nothing to yank` until the next key; in the help card. Scoped to the last reply — "a picked message" waits for a message picker to exist.** | claude | done |
| 6.21 | An analysis brief must not modify the workspace. Second drive: "have someone check" error-handling consistency became four unsolicited edits to `arc-core/src/log/` in the real working copy — sound edits, verified after the fact, but nobody asked for them, and the poke storm ("Complete check and state conclusion") is what pushed the executor from reporting into acting. The grant machinery already distinguishes read from write; dispatch just never says which the job is doing. A required `intent` enum on dispatch (analyze = project root granted read-only, implement = read-write) turns the existing gate into the policy, with the brief's wording as backup. Decide shape before building **`intent` is required with both values taught in the schema; analyze downgrades every grant to read-only at child creation — all of them, not grants[0], so the policy cannot ride on ordering convention. Nothing new in the log: the recorded read-only grants are the durable record, and a resumed analyze job stays analyze because 5.1 already replays recorded grants — tested against a config that says read-write. `bash` keeps running in the project root whatever the mode (`read_write_root` became `project_root`), since bash is policy, not containment, and an analyze job still needs `cargo test`. `identity.md` gained the orchestrator rules — dispatch then wait, continue rather than redispatch, analyze means look, handbacks are claims, conclusions not transcripts — written by Bogdan's explicit delegation this once; invariant 7 stands.** | claude | done |
| 6.22 | Cost lines survive history refetches. 6.7's line renders and then dies in milliseconds: `Block::Cost` exists only client-side, our own turn's appends broadcast `session_appended`, the suppression flag clears at stream end, and the queued notification's refetch rebuilds the transcript from server history — wiping every cost line. In a session where jobs hand back, no cost line is ever visible. Either preserve client-only trailing blocks across a rebuild or make per-turn usage durable and render it from history (7.3 wants usage by role anyway) **Durable, decided by Bogdan: `MessageAppended` 6–8 and `HistoryMessage` 5–7 carry input/output tokens and elapsed ms, stamped in `append_reply` — the one site all three turn kinds funnel through — on the turn's final assistant message only; zero means absent, so old events render as before. Projection bumped to v10 (the spec said 10→11 off a misread version; the agent caught it), columns filled on replay. The live `End` push stays for instant feedback with client latency; the refetch replaces it with the server-timed line. One narrow call, flagged by the implementing agent and kept: a turn cut after a completed tool step stamps its accumulated usage durably even though the live stream showed none — the refetch shows a cost line the stream never did, which is the truth.** | claude | done |
| 6.23 | The job environment lacks the dev toolchain. The executor's `cargo clippy` failed on missing `protoc` — arcd's service PATH has no devshell, and the job's tests only passed because the checkout's `target/` was already warm. A week of development through ARC needs jobs that can build from cold: either `bash` enters the devshell (`nix develop -c`, slow first call) or the service environment carries the tools **Per-project wrapper, decided by Bogdan: `[projects.*].command_prefix` (argv list, default empty) that `bash` prepends to its child — `nix develop -c bash --noprofile --norc -c <cmd>` for `arc`. It re-derives per turn from config exactly like tool sources, never logged: execution environment, not containment, and a vanished project contributes no wrapper. Process-group kill, env scrub, caps and drain all apply to the wrapper's whole tree unchanged; a missing wrapper binary is a named tool error. The wrapper binary itself must be on the service PATH — `nix` is, from the system profile.** | claude | done |
| 6.24 | `:jobs` rows print raw token counts (`441266/- tok`) where the strip prints compact (`41.2k`); use one formatter **`format_tokens` widened to u64 and used by the strip, the row's spent, and the row's budget; sub-thousand counts stay bare so the small-number tests read unchanged.** | claude | done |
| 6.25 | The picker separates conversations from jobs. Today it lists every session as a peer — `SessionInfo` carries neither role nor project, so the client cannot even tell a dispatched child from a conversation. Additive `role` and `project` fields on `SessionInfo` (own schema commit); the picker opens showing conversations only, `a` toggles everything with job rows dimmed and tagged `role/project`; compounds with 6.9's `/` filter and 6.2's titles. Jobs older than `:jobs`' 600s aging stay reachable through the toggle **`SessionInfo` 6–7 carry role and project; the picker filters on the client so the toggle is instant. `a` binds only inside the picker (normal-mode `a` still inserts). Job rows keep their titles, dimmed, tagged `role/project`. Rode one code commit with 6.26 — the two interleave in one app.rs hunk.** | claude | done |
| 6.26 | Visual-mode yank, vim-shaped: `V` in normal mode enters visual with the selection anchored on the last block, `j`/`k` move the boundary block-wise, `gg`/`G` take it to either end, `y` yanks the selection, `Esc` leaves; `Y` yanks the whole conversation without entering visual. Yanked text is plain: `you:`/`arc:`/`system:` headers, thought/tool/cost chrome skipped. Rides 6.20's OSC 52 path **Anchor and boundary are indices captured at `V`, clamped before every use, and visual exits on any transcript rebuild (history refetch, empty-reply pop) — appends never invalidate them. Selection renders reverse-video; the boundary block auto-scrolls into view; the rule line says `visual`. `Y` needs no visual entry.** | claude | done |
| 6.27 | Handbacks fold like thoughts: the raw job summary drowns the conversation when the narration right under it is the readable conclusion. Render a handback collapsed to one dim line, expanded by the same toggle that opens thought traces. First feature developed through ARC itself — the executor implements it, the failures get documented here **Landed as the executor wrote it, verified from scratch: a `Handback` block folded by default, `± {subject}` dim, ctrl-o toggles thoughts and handbacks together, ~11 tests. Detection parses `record_handback`'s exact `Job {uuid} finished/stopped` shape with uuid validation so prose never folds — a deliberate string coupling the client documents; a wire marker can harden it later. Yank skips folded handbacks; the narration is the yankable conclusion. The road there produced 6.28–6.33: three 8-step ratchet cycles, a restart, one stale forcing steer, and a 64-step turn of 1.40M in / 47k out over 29 minutes at the endpoint's ~33 tok/s. The model self-reported a transient overwrite of `format_yank` it repaired — the shipped function is byte-identical, verified. Cheap-executor verdict for the phase bet: it got there, with expensive exploration and one human shove; the code that arrived was correct and tested.** | claude | done |
| 6.28 | A coding turn gets more than eight tool steps. Found ten minutes into 6.27's drive: `MAX_TOOL_STEPS = 8` cut the executor mid-exploration three turns running — the forced tool-less step (built for the concierge) made it narrate "let me read the relevant sections" as its final reply, the handback delivered the fragment, and the concierge correctly nudged with `continue_job`, producing an 8-step ratchet: functional, bounded by the autonomy counter, and three times the cost per step of progress. The cap is now per-role — executor 64, everyone else 8 — because the two turn shapes are genuinely different: a concierge turn is dispatch plus a memory op, a coding turn is an exploration. The ratchet remains as the backstop past 64 | claude | done |
| 6.29 | The strip answers "is it doing anything?": show the running job's step count and seconds since its last engine event. Live incident: a job sat at `0 tok · 1102s` while alternately generating 5k-token completions at the endpoint's measured ~33 tok/s, running tools, and being genuinely idle — indistinguishable without journalctl and ps. Tokens update only at turn boundaries, so a long turn shows zero all the way through **`JobStatus` now tracks `tool_steps` (one per `ToolCallStarted`) and an idle clock touched by every engine event; each started call broadcasts a `job_changed` push, so the strip ticks `step 12 - 8s ago` forward locally between pushes. `run_turn` drains buffered events when the send resolves, because a fully-scripted turn can finish with events still queued — fast tool calls otherwise never count. Read-only touch events (deltas, reasoning, tool ends) reset the idle clock without a push.** | claude | done |
| 6.30 | Reconnect is send-only: after a daemon restart the client faults once, then browsing (`:jobs`, picker, refetch) silently does nothing against the dead socket — a client left open across a deploy shows a stale world with no banner. Reconnect on any command, or at least surface "disconnected" persistently in the rule line until reconnected **The net loop already re-dialed on any command — the finding that mattered: the app never cleared `Status::Disconnected` except through a queued send, so a successful browse left the banner lying. One guard clears it on any non-disconnect event; the indicator went dim per spec; proven end-to-end with a real drop-and-relisten socket test.** | claude | done |
| 6.31 | Executor thinking is uncontrollable on an `openai_compat` role: config rejects the `thinking` key there by design (no measured field), so glm-5.3-flash freely spends 3–6k output tokens thinking between edits at ~33 tok/s. Probe what OpenCode Go actually accepts (`reasoning_effort`? `thinking`?), then allow the key for measured endpoints — or conclude the executor model choice is the lever and record it in providers.md. **Measured 2026-08-27 on deepseek-v4-flash: Go accepts `reasoning_effort`, and `"none"` works — 2 output tokens against 16–20 default on the same question, correct both ways, stable over three runs (one transient empty reply from Go, unrelated). glm untested; we left it. Remaining: let an `openai_compat` role carry `thinking` mapped onto `reasoning_effort` the way Gemini already maps it — but whether the executor should ever run at `none` is a separate question; thinking may be exactly what a coding turn needs. Default stays untouched until a real job is measured at each level. **Config now accepts `thinking` on openai_compat; the serializer maps Minimal→"none", Low/Medium/High→their names, Default→key absent (byte-identical wire, cache-stable, test-pinned). The executor stays on Default; per-level measurement on real jobs remains open and rides 7.3.** | claude | done |
| 6.32 | The role pin leaks config changes into old sessions. 3.2 pins by role alone, so editing `[roles.executor].model` (glm → deepseek, first done 2026-08-27) makes every finished executor session resume under a different model on its old transcript — a mid-session swap through the back door, against invariant 9, cache discarded silently. `SessionCreated` already records provider and model (2.1); check them on resume and refuse or warn when the role's runner no longer matches, with a fresh dispatch as the escape **`identity_mismatch` is one check behind both entry points: direct sends refuse with `model_mismatch` on the wire, and a `continue_job` naming a stale session refuses synchronously in-turn, so the model reads the reason instead of a later failed handback. Pre-stamping sessions (empty recorded identity) stay unpinned, mirroring 3.2's UNSPECIFIED rule. Projection v11 adds the provider/model columns; the rebuild backfills every historical session from its recorded event. Enforcing it exposed ~40 tests driving a session with a runner named differently from its recorded identity — aligned rather than masked.** | claude | done |
| 6.33 | Steers are uncancellable against long turns. A forcing steer composed at step 10 of a 64-step turn landed 20 minutes stale, after the work was done — and because the handback carries the child's final reply, the stale steer's answer displaces the real report as the summary. Options, smallest first: show queued steers in `:jobs` with a key to drop one; or hand back the last substantive reply rather than literally the final one. 6.28 made turns long; this is its bill **The visibility arm landed: `JobInfo.queued_steers`, tracked at queue/consume/drain with a `job_changed` push each move; rows show `· {n} queued`, `d` drops them all — the drop is signalled to the job's own task rather than done synchronously, because a supervisor-side drain raced a steer queued between request and drain. The last-substantive-reply arm stays deliberately open; with the queue visible and droppable the incident cannot recur silently, and choosing summaries needs more live evidence.** | claude | done |
| 6.34 | A daemon restart loses live jobs' handbacks: the 12:03 restart killed the folding job mid-turn — orphan repair mended the log, but the parent conversation still ends with the continue ack, and the concierge believes a job is out forever. 6.17 covers a job that dies; this is the daemon dying around it. On startup, every session that was a live job (recorded role, unfinished last turn) hands back "stopped: the daemon restarted", or the supervisor resumes it — decide which. The restart also ate the 5.4 steer queue silently **Hands back, per the spec's simpler arm: `repair_restart_handbacks` runs once after orphan repair, finds jobs via the new `SessionCreated.dispatched_by` (the dead `parent_session` column finally earns its keep — filled by a fresh field, the abandoned forking design's numbers stay reserved), skips jobs whose parent already holds a `Job {id} …` conclusion, and routes through the ordinary `record_handback` — turn guard, narration turn, everything. Idempotent across restarts by construction, tested. Jobs dispatched before the field exists are unrepairable and skipped; they age out of relevance.** | claude | done |
| 6.35 | Visual mode survives appends: every `session_appended` refetch rebuilds the transcript and 6.26's exit-on-rebuild drops the selection — constant in any session with a live job. Keep visual when the rebuild is append-only (new transcript ≥ old length, prefix blocks equal), exit only when the prefix truly changed. Found by Bogdan in live use within hours of landing **`starts_with` over the old blocks decides; the selection indices stay valid by construction. A prefix that differs — a client cost line replaced by the durable one, a toggled fold — still exits, accepted: the fix targets the constant case, appends from a live job.** | claude | done |
| 6.36 | `arcd/src/jobs.rs` is 3,419 lines and the executor pages it 120–400 lines per `read` call: both models spent 15+ steps just seeing the supervisor before touching it — half a 64-step turn on one file, every time. Split it into agent-sized modules (supervisor, watchdog, steering, handback are already distinct machines living in one file). The codebase being navigable by its own executor is a phase requirement, not a style preference **Pure move into `jobs/{mod,turn,handback,status,prompt,tests_common}.rs`, no file over 1,200 lines, tests living with their machinery. Executed by the no-retyping procedure: a gap-free partition table, sed-driven extraction, only mod/use/visibility lines authored — the diff's +208 net is exactly that wiring, 804/804 tests on both sides, public surface untouched (daemon.rs and server.rs needed zero edits).** | claude | done |
| 6.37 | A resumed job's spent counter restarts at zero (6.16 noted it, live use vetoed it: a two-turn job read `0 tok` for twenty minutes while spending 100k+). 6.22 made per-turn usage durable, so seed the counter from the child session's summed usage on resume instead of accepting the reset **`route_continue` sums the child's durable usage in one projection query and seeds `JobStatuses::start`; fresh dispatches still start at zero.** | claude | done |
| 6.38 | The strip scopes to the open session: watching a job session, the strip redundantly lists that same job — it should list the session's own children (what this session dispatched), which is also the right meaning in a concierge conversation. Bogdan's call from live use **`JobInfo.parent_session`, filled for live and terminal entries; the strip requires `parent == open session` and a running state, so a job session shows its own grandchildren and no session shows nothing. The `:jobs` popup deliberately stays global.** | claude | done |
| 6.39 | A job can be cancelled from the TUI. When the 6.29 job went bad the only lever was restarting the daemon: `:jobs` has no kill key, budgets are suspended, and the concierge's autonomy re-nudges a failed job. A `k` on the jobs row that stops the task through the supervisor and hands back "stopped: cancelled" **A watch-channel cancel arm in the turn select — it fires even mid-tool-call, where the silence timeout defers — drops the running turn in 6.17's blessed partial shape, drains queued steers with the counted warning, and hands back "stopped: cancelled by the user." through the ordinary path, which is also what makes 6.34's restart repair skip it (a conclusion is a conclusion). `k` in `:jobs` cancels (Up-arrow keeps upward navigation), terminal rows no-op with a footer note. Cancel and drop acks use the single-frame verdict shape, not steer's turn shape — these are actions, not conversation.** | claude | done |
| 6.40 | Jobs and version control. The 6.29 executor ran `git commit` on a detached HEAD because the brief said "schema stays its own commit" — it tangled the colocated repo and its work needed salvage by hand. First instinct was a prohibition in the dispatch schema; Bogdan overruled it: the harness forbids nothing, VCS knowledge lives in AGENTS.md (jj not git, path-scoped commits, commit-on-request), which every job now receives as its system prompt — so "commit this" works as a plain instruction in a brief, steer, or continue, and he dictates when. **AGENTS.md gained the Version control section; the dispatch description stays policy-free.** | claude | done |
| 6.41 | Executor parity investigation — why pi is faster and cleaner on the same model, measured not guessed. Three confirmed harness gaps: the executor has no system prompt at all (`system: None` — the brief is its only steering, where every surveyed harness front-loads workflow guidance); reasoning is discarded between steps by design, so a thinking model re-derives its plan every step; and Go reported no cached-token field in any probe, so each step may pay full prompt processing over a growing context. Measure caching on a real transcript, draft a minimal executor system prompt, test reasoning replay on the compat API — then decide what lands **Investigated against pi's public source (badlogic/pi-mono, read directly) and DeepSeek's docs; four things landed. Caching was never broken — Go caches automatically (15k prefix: 2.0s cold, 1.0s hot, `cached_tokens` reported) and arcd now logs `prompt cache hit` per completion. The real defect chain: DeepSeek's tool-calling contract requires each step's `reasoning_content` replayed within the turn, Go streams it under `reasoning` where our parser never looked, and we discarded it besides — so the model re-derived its plan every step at ~33 tok/s. Now: the parser takes the first non-empty of three aliases, the turn loop carries reasoning on in-memory assistant entries and serializes it back as `reasoning_content` whenever tools are present (empty string included — the contract wants the key), and the log stays reasoning-free, proven by a rebuild test, so the invariant holds and reasoning does not cross turns. Jobs get a system prompt at last: a ~55-token preamble plus the project's AGENTS.md verbatim, built once at spawn, byte-stable — pi ships ~200 tokens and trusts RL training; the executor was burning tool steps fetching the conventions we now inject. Tool shaping: `read` pages 2000 lines / 48 KiB (was 400 — a 3,400-line file cost 15+ calls), and `bash` keeps the LAST 16 KiB instead of the first, because cargo puts the errors at the end — the executor literally could not see a failing build's failures. Known gap, accepted: typing directly into a finished job session (6.14's path) still runs promptless. Compaction stays deferred per DESIGN — pi has it, our jobs haven't hit a window. Acceptance is the next live drive on a jobs.rs-shaped task.** | claude | done |

| 6.42 | A cancellation is not a failure to route around: the first live `k` produced a perfect cancel handback — and the concierge's reaction turn re-dispatched the job it had just watched the user kill. Words, two places: the cancelled handback text should say the user chose to stop it and it must not be restarted unless asked, and identity.md's job rules should carry the same line (Bogdan's edit by invariant 7, or delegated as with 6.21) **Both lines written — the handback reason now carries the instruction inline where the reaction turn reads it, and identity.md's job rules gained "a job cancelled by the user stays stopped" (delegated again, as with 6.21). Whether words hold is the next live cancel's question.** | claude | done |

## 7. Production

| # | Task | Assignee | Status |
|---|------|----------|--------|
| 7.1 | `arcd rebuild` proven against the real log: drop the index, replay, diff against live state. The phase contract names this explicitly | — | todo |
| 7.2 | Installed for real: systemd user unit enabled, release binary on a stable path, runtime state surviving a rebuild of the machine. Phase 1 left the unit installed but not enabled. Move runtime state out of the repository to XDG paths — `~/.local/state/arc/` for the log, index, and secrets, `~/.config/arc/arc.toml` for config — so that arcd's own keys stop living inside a project root the workspace tools can be granted. DESIGN.md and AGENTS.md already name the installed layout; this task is the code and the migration | — | todo |
| 7.3 | Account for token use and latency by role from existing spans. Use the measurements to replace published-rate estimates in `providers.md`. | — | todo |
| 7.4 | Phase 1 leftovers: `log::Error::Io` field doc, a retention policy for `data/traces/`, and the `TraceCapture` test flake (a `set_default` race across parallel test threads, ~1/100). | — | todo |

## 8. Memory

Opened 2026-08-27 after live use showed the reviewer producing tens of duplicate candidates and facts the record index already holds. Consolidation is disabled in config (`[consolidation] enabled = false`) until this section is worked — the log keeps everything, so nothing is lost by pausing extraction.

## Decisions, 2026-08-28

Taken after a research day over pi (dev-branch rewrite), hermes-agent (delta since the pinned read), T3 Code, Letta, Mem0, Graphiti, OpenClaw, the product memories (ChatGPT, Cursor, Claude.ai), and the papers behind them, plus a reconstruction of our own flood from the log. The reports were session artifacts; what binds is folded here and into the rows.

- **Curation moves from prompt to code.** Every surveyed system that survived real use stopped trusting prompt-level "check the index first" and put dedup behind deterministic gates with model judgment inside them. Published confirmation the gap cannot be prompted away: bounded-memory supersession failure does not close with model scale (arXiv 2606.27472).
- **The writer holds the state it changes.** ChatGPT injects every saved memory before the save decision; Letta and pi rewrite state held in context; hermes refuses a write whose target was not freshly read — 32 of 32 background writes silently rejected over 48 hours before their prompt taught the guard. A one-liner index is too thin to answer "already captured?".
- **Forced-choice, not open judgment.** Retrieval per candidate fact, then a decision among concrete neighbors with a tiny structured output. Mem0 removed that decision step in v3 ("when in doubt, extract") and its tracker documents the resulting floods — eight near-identical memories in an hour.
- **Nothing-to-save is the expected, rewarded outcome.** Conservative extraction polarity; extraneous ops count as failures. Our own purge is what an optimistic extractor costs: a flooded queue made wholesale deletion the rational review action, and the genuine preferences went out with the junk.
- **The queue triages risk, not volume.** Wrong facts poison — models cite them and double down; boring facts filter at read time (Cursor, measured in production). Contradictions and supersedes reviewed first.
- **The archivist model is a lever, not a commitment.** The role stays on the 8B until 8.7's numbers say otherwise; `[roles.archivist]` already reaches OpenCode Go by config.
- **The concierge is the primary writer of user-stated facts.** The conversation model with the index in context, at the moment of "from now on", is the best-placed writer — the shape ChatGPT ships. The archivist is the sweep behind it, not the primary.

| # | Task | Assignee | Status |
|---|------|----------|--------|
| 8.1 | Diagnose the duplicate flood: is it the sidecar model, the extraction prompt, or missing dedup against the existing record index? Read the actual candidates from the paused queue before changing anything — the failure evidence is already durable **Reconstructed from the log: 67 memory events over six days — 15 creates, 17 supersedes, 32 review deletes, 3 accepts; the index now holds zero records. Five sources, by volume. Consolidation has no role filter, so executor job transcripts got a prompt asking what the user revealed about themselves — unanswerable, and roughly 70% of all ops are the resulting task-progress records, one Conway record superseded eleven times across scratch jobs, each recording the file's latest state. Supersede-as-touch: byte-identical re-supersedes that satisfy check-the-index by churning. True dedup failure: a fresh create of "User prefers concise replies" while its ACTIVE twin sat in the rendered index. Meta-conversation records about the act of conversing. Identity-redundant records: the extractor never sees identity.md, so it re-extracted the user's name. The v1 prompt was not the bottleneck — it already carries the full hermes rulebook and the model violated every rule of it.** | claude | done |
| 8.2 | Extraction is gated by role: the consolidation pass extracts only from concierge sessions; titling keeps running for every session, since job rows need names. A job's durable record is the archive and its handback — a work transcript holds no user facts, and Cursor measured what a model does when asked anyway: it writes a task log **`SessionSnapshot` carries the recorded role and one rule decides in both places that extract — the live pass and memory-replay, so replay cannot diverge from what the daemon would do. Concierge and pre-role sessions extract; executor, archivist, and any unknown future role fail closed to titling plus the marker, so gated sessions still stop being due. A gated session shows up in a replay report with zero operations rather than vanishing. Proven by call-counting: the executor-session test asserts the extractor is never invoked while the title still lands.** | claude | done |
| 8.3 | The extractor sees what is already known: identity.md rendered into the extraction input as context that must never be re-extracted, and recalled memory — memory tool results in the transcript — excluded from extraction input. OpenClaw's rule: a fact recalled one hundred times stays one fact **An `[Already known — never extract]` section between transcript and index carries the identity text, outside the transcript window so a long session cannot crowd it out; the daemon clones the loaded identity before it moves into role resolution, and the memory-replay CLI loads it the same way so replay renders what the live pass renders. Results of the six recall tools — the four memory tools plus `sessions_search`/`session_read` — render as a marker instead of content, matched to their call by id; write acks count as recall too, so an ack's echo can never be re-learned. The call lines stay, so the pass still sees what was done.** | claude | done |
| 8.4 | Prompt v2: the priority ladder with contrast pairs (mistakes and corrections, then preferences, then facts), worth-saving defined as "would change future responses in similar situations", the present-tense test, and the phrasing rules that make lexical dedup work — self-contained, pronoun-free, proper nouns kept, never generalized, dates absolute against the session's date. Kept terse: a background pass runs on a low attention budget. Gated by memory-replay against the sessions that produced the flood — the regression data is already in the log **v2 leads with "most conversations contain nothing durable" and "a needless record is a failure", teaches the ladder, the save criterion, the phrasing rules, and the already-known section 8.3 added; the output contract is byte-identical to v1 so the parser is untouched, both versions stay in KNOWN_VERSIONS, and both are pin-tested. The replay gate ran against five flood sessions on the live sidecar: v1 under the new input path already fell from ~15 historical ops to 2 — the code gates did that — and v2 produced 0, refusing the one survivor because identity.md's voice rules already cover conciseness, which is the refusal the prompt teaches. Whether v2 stays this quiet on a genuinely novel fact is 8.7's live question; no session in the log states one that was not also written inline.** | claude | done |
| 8.5 | Mechanical dedup between parse and events: each candidate op is normalized and searched against the ACTIVE records; an exact match dies in code as a no-op; near matches go to one forced-choice call — a schema-forced reasoning field first, then duplicate-of/supersedes/neither as integer indices into the shown neighbors, every id validated and the supersede applied by code. Graphiti's shape, with the measured small-model fix: reasoning-before-decision took a nano model from 1/9 to 14/15 on contradiction cases | — | todo |
| 8.6 | The concierge writes user-stated facts inline: `memory_write`'s tool description gains the save criterion and the skip list (overly-personal, short-lived, random, redundant), so the explicit path carries the load at the moment a fact is stated. Any identity.md reinforcement is Bogdan's by invariant 7 **Both descriptions, since the pair is one policy: `memory_write` carries "the moment the user states", the would-it-change-future-replies criterion, the four-part skip list, and the phrasing rules that make lexical dedup work (self-contained, present tense, names not pronouns, dates absolute, proper nouns unrounded), plus a pointer to supersede when the index already holds the fact. `memory_supersede` gains the default-verb line — a changed fact is a supersede, never a sibling — and its guard against supersede-as-touch. Tool descriptions are sanctioned context per invariant 6; no code change, prefix stability untouched.** | claude | done |
| 8.7 | Consolidation back on, measured: duplicate-rate at review (every dup verdict is a free label), records per session, supersede precision, retrieval hit rate — the numbers that decide whether the 8B keeps the role or the archivist moves to OpenCode Go | — | todo |

## State at the end of 2026-08-26

Sections 4, 5, and 6 are done — one day, forty-five commits from `21249d24` to `fba55bfe`. The substrate (registry sources, confinement, workspace tools), the job loop (dispatch, supervision, steering, handback), and the closed loop (the handback turn, `continue_job`, hygiene) all landed with the reasoning folded into the rows above. Budgets are suspended for calibration (see 5.5 and DESIGN §4.1); the projection is at version 9 and rebuilds itself on first start.

Next session: a deliberate test drive — Claude driving the TUI in tmux, putting the whole loop through its paces and folding every failure into this list. Known standing items for that drive: the restart-after-rebuild gotcha bit twice (a `just deploy` recipe that builds and restarts in one motion is still unwritten); 4.7's token re-measure is now urgent since handback turns changed the always-on load; and `identity.md` has no orchestrator guidance yet — briefs, verifying handbacks before believing them, reporting conclusions not transcripts — which is Bogdan's edit by invariant 7.

## The second drive, 2026-08-27

Three dispatch cycles through the TUI, watching the daemon from the journal and the projection. What worked: grounding (3/3 to `arc`), supervisor queueing under every poke burst, the handback as a dim system block, clean narration turns, the live strip with token counts, `:jobs` with 6.17's aging, `Enter` into a child and `Ctrl-t` back, elided tool lines making a child transcript readable. What failed went into rows 6.19 (widened: pokes fire at dispatch, multiply cost, and displace real `continue_job` use) and the new 6.21–6.24. One executor overstatement worth remembering when writing the identity.md guidance: a handback claimed a rule was "triplicated across format.rs, writer.rs, and dir.rs" when writer.rs never had it — the child's own transcript said three call layers, the summary garbled it into three files. Verify handbacks against the diff, not the prose.

## Exit criteria

Verify these live, not only in the test suite:

- [ ] A week of real development on ARC done through ARC, not through another harness.
- [ ] `arcd rebuild` reproduces live state from the log.
- [ ] A job runs to completion while the conversation stays responsive, and reports back.
- [ ] A call to a tool the session does not hold reaches the model as an error it acts on, and the declared sources survive a restart.
- [ ] The TUI can refresh a running job's status and shows its final handback.
- [ ] Measured token use and latency are visible by role in traces.

## Not in this phase

- **A sandboxed worker.** The honest replacement for the approval gate we removed, and the change that would make `bash` genuinely contained. Phase 3.1, once jobs are real enough to say what the sandbox has to allow. A grant over the whole home directory waits for it: arcd's own keys live under `~/.local/state/arc/`, so a wide grant puts them back inside a project root, and an exclusion list cannot help when the shell never consulted one.
- **Provider fallback ladders.** Add them only after real outage or spend data justifies their state and recovery rules.
- **Expert consultation and MCP sources.** The spike and source interface guide later work; neither is in the initial job path.
- **Web tools of our own.** Cut on 2026-08-24 rather than deferred. Gemini grounding covers the concierge and `bash` covers the executor, so nothing is left for `web_search` and `web_fetch` to do. 4.1 keeps the source variant; 3.7 carries the work that would make it real.
- **Built-in to-dos.** A file in the workspace is the whole feature. Do not add a tool for it.

- **Forking, rewind, tree navigation:** Phase 3.5. Wanting them now makes them next, not part of this phase.
- **Voice:** Phase 4. Nothing here may assume an audio client.
- **Devices:** Phase 5. Add an MCP source with the first real device.
- **Compaction as an event** (DESIGN.md §12): wait until a real job reaches a context limit. This schema change needs evidence.
- **Prompt v2:** wait until extraction quality warrants it. Its gate remains two pinned regression cases and `memory-replay --against`.
- **Batched log appends:** wait for traces showing that fsync per append costs enough. Jobs will provide that evidence or settle the question.
