# TASKS — Phase 3.5, tree

This is the live list. `TASKS-phase1.md` through `TASKS-phase3.md` are historical records. `DESIGN.md` defines the phase: session forking with the §4 branch semantics, rewind, and tree navigation in the TUI — split out of Phase 3 and kept immediately after it because rewind is a development feature: recovering from a bad edit path without re-prompting from scratch is what makes a cheap executor affordable.

**Phase goal:** branching gets used naturally. **Carried gate:** met 2026-08-29 — the proving week ran on real work, the computer-enhance drives closing it, and Phase 3's last exit criterion closed with it. Phase 3.5 itself is built directly, not through ARC.

The 0.x rows are memory hardening out of the 2026-08-29 comparison against pwning.systems' Lemmalog post (`llm-memory-program-analysis`), run twice — once from DESIGN, once by an executor reading the code — with both reads converging on the same first adoption. The post's Datalog engine itself is deliberately not adopted: supersede plus provenance already covers most of what its fixed point buys, at none of the machinery.

Bogdan assigns each task; the other reviews it. Statuses are `todo` → `in progress` → `in review` → `done`. Assignees below are intentionally unset.

| # | Task | Assignee | Status |
|---|------|----------|--------|
| 0.1 | Stale dependents reach the review queue: superseding or deleting a record stamps `changed_at` on ACTIVE records that link to it, re-entering them in the weekly review — deterministic flagging, human verdict, no new events or UI. Decided here: shared provenance sessions do *not* count as dependents — one busy session would co-flag everything it produced, and queue noise is the failure mode §5.4 watches; links are the deliberate signal. Built as: the stamp inside both projection apply paths (forward-only, `last_event_seq` untouched); prompt v4, whose only diff from v3 is the links sentence — link what this fact leans on — with the model's links validated against the shown index so a hallucinated id never enters the graph. Before the daemon deploy: a `memory-replay` v3-vs-v4 diff over real history, per §5.4's tuning loop | claude | done |
| 0.2 | Memory as of time T: a projection query answering what was ACTIVE at a timestamp. Built as `memory_active_at` on the projection and the reader, over two new columns (`created_at`, `superseded_at`; schema 13, rebuilt by replay): a record is held on [`created_at`, `superseded_at`), in its last-known form — content reconstruction stays the log's job. Decided here: a purged record is absent from history too, which is the point of a purge; updates and in-place supersedes keep their activation time; an event missing a timestamp excludes its row rather than guessing. No proto change | claude | done |
| 0.3 | Rank `memory_search`: today it is LIKE word-matching over ACTIVE records ordered by namespace/kind/title. Move distilled records onto FTS5 with bm25 (the stack already carries FTS5 for `sessions_search`), keeping the LIKE path as fallback when FTS finds nothing — LIKE matches inside words, FTS does not, so the fallback preserves today's recall under the new ranking. 0.2's `as_of` rides in here as an optional tool argument: the FTS index covers superseded records too, filtered by status or interval at query time, so "what did you believe about X in March" is one search. Also the honest precursor to the deferred sqlite-vec decision in DESIGN §5.3. Built: `memory_fts` (schema 14) over all records, synced in the apply paths only when a write lands; bm25 weights title 4, summary 2, body 1; superseded hits flagged `superseded: true`; `as_of` accepts RFC 3339 or a bare date read as that day's end | claude | done |
| 1.1 | The proving week: every row below is developed through ARC — dispatch, `:code`, steers, handbacks. Failures fold into this list the way the drives always did. Closes Phase 3's last exit criterion when seven days of real work have run | — | done |
| 1.2 | The expert role — counsel, carried from Phase 3 at Bogdan's call: spike 1.2's two verified read-only templates (`claude -p` and `codex exec`, argv preserved in TASKS-phase3.md section 4) become a real part of the harness. Decided 2026-08-29: both shapes at once — a `consult_expert` registry tool behind a new `expert` source, configured by `[roles.counsel]` resolving to a command template (never a `Provider`, per DESIGN §6.2; the CLI runs its own read-only loop over the real workspace, which is the point). The counsel row joins §6.1's table as the fourth role but never holds a session. Both the executor and the concierge hold the tool, resolved from the role whenever counsel is configured; the tool takes a required dispatch-style `project` argument — `"none"` means the bound session's own root, the concierge names one. Output lands as ordinary tool call/result events; usage and latency go to spans; counsel spend is subscription-side and invisible to job budgets. Default command claude (its tool surface closes; codex's `web__run` does not). The child inherits the user environment — subscription auth needs HOME — an explicit exception to the bash scrub, stated in DESIGN | claude | in review |
| 1.3 | Web search for the concierge: Gemini grounding through 4.1's `web` source, the capability the 2026-08-24 decision cut our own tools for. Measured then: Search and Maps grounding work on the native API on 3.6, compose with `minimal` thinking, and combine with our function tools once `toolConfig.includeServerSideToolInvocations` is set — in one round trip the model searched, circulated the result, and called our function with an argument derived from it. Outstanding, from 3.7's close: the `toolCall`/`toolResponse` parts need a proto change and a place in the transcript (schema commit, its own row when designed); attribution rendering in the TUI, since citations are the point; per-query billing watched in spans. The `web` source resolves from the role, not project config — an unbound concierge is exactly the session that needs it | — | todo |
| 2.1 | Fork schema, its own commit: the abandoned forking design left reserved field numbers on `SessionCreated` (see 6.34's note) and `fork_point`/`parent_session` columns already in the projection — reuse or re-reserve deliberately, and record what a branch is: parent session, fork seq, and whether it is *real* (feeds consolidation) or scratch | — | todo |
| 2.2 | Fork and rewind in the engine: create a branch from a message, continue it under the same role and provider — same cache economics as `continue_job`. Rewind is fork-at-an-earlier-seq plus opening the branch; no history is ever rewritten (invariant 1) | — | todo |
| 2.3 | Tree navigation in the TUI: see a session's branches, jump between them, mark a branch real or abandoned. Compounds with the picker and `Ctrl-t`; the visual language should stay ASCII-minimal like everything else | — | todo |
| 2.4 | Branch semantics meet memory: only the main line and branches marked real feed consolidation (DESIGN §5.4); abandoned branches stay searchable in the archive and never write distilled memory. The 8.2 role gate composes with this — decide the query, not new machinery | — | todo |

## Standing watches, carried from Phase 3

Not tasks until evidence arrives: whether the continue-vs-dispatch and cancelled-stays-stopped words keep holding; whether inline memory writes keep their stated scope (the next judgment miss triggers the 3.7-low concierge trial); 8.7's week of dedup and review-queue numbers; 9.9's edit-tool friction; 9.2's grant-widening arm; compaction the first time a real job exceeds its window (DESIGN §12 — it becomes a schema task the day it happens). From the Lemmalog comparison, two more: whether reviews surface extraction misses, not just bad writes (a fourth trace metric the day they do), and whether records about one entity accumulate under different names — the post's largest front-end win, not worth a pass at our record count until it visibly bites.

## Not in this phase

- **A sandboxed worker** — still the honest replacement for the approval gate; revisit once tree work says what the sandbox must allow.
- **Voice (Phase 4), devices (Phase 5), embeddings** — unchanged.
