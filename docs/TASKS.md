# TASKS — Phase 3.7, the direct door

This is the live list. `TASKS-phase1.md` through `TASKS-phase3-6.md` are historical records. `DESIGN.md` defines the phase under sections 1, 4.1, 4.4, 5.4, 7, and 11, all amended 2026-09-03.

**Phase goal:** the executor becomes the session the user develops in. The quiet week's own numbers decided it: seven of thirteen rows repaired what the concierge lost, cut, or invented between Bogdan and a job, and the doctrine constant grew to thirteen lines patching a relay that cannot read the code. The concierge keeps the conversation, memory, grounding, and away-from-keyboard dispatch, which is the shape voice needs. Development moves to the bound session, which needs to feel like the harness it replaces: messages land mid-turn, tool output is on screen, a long day compacts instead of dying.

Order matters here. The runner (row 1) is the simplification the rest stands on; compaction (row 4) is the one nothing else waits for and the one a real session hits first; the door (row 6) is the cheapest and goes last.

**Exit criterion:** a week of development in `:code` sessions, the concierge used for talk and for dispatch from away, and compaction having fired on real work without a visible loss.

Bogdan assigns each task; the other reviews it. Statuses are `todo` → `in progress` → `in review` → `done`. Assignees are intentionally unset.

| # | Task | Assignee | Status |
|---|------|----------|--------|
| 0.1 | Carried from 3.6 row 2: identity follows the user; jobs doctrine moves to code. In review there; the record has the detail. | claude | in review |
| 0.2 | Carried from 3.6 row 3: project picker for `:code`. In review there; the record has the detail. | claude | in review |
| 1 | One runner for every session. Today a concierge or direct executor turn runs on the connection that started it and streams back through that socket, while a job turn runs as a supervised task with its own step loop, steer queue, handback, and footprint (`arcd/src/jobs/turn.rs`, `server.rs` `forward`). Every turn now runs as a supervised task keyed by session; a connection subscribes to the session's events and can drop and return mid-turn. The engine's step loop is already shared; what moves is who drives it and where the stream goes. Decided: the runner is per session with one live turn at a time, the same lock the job supervisor holds today; the wire keeps its shape, so the TUI sees the same `MessageAccepted`, deltas, tool events, and `StreamEnd` it sees now, only sourced from the task rather than the connection. A job becomes a session whose parent gets a summary and footprint when its turn ends; `jobs/handback.rs` shrinks to that. No proto change expected; if reconnecting to a live turn needs a subscribe message it is its own schema commit. | | todo |
| 2 | Messages into a live session land at the next step boundary. Jobs already do this for steers (`steer_live`, the queue in `turn.rs`); the interactive session gets the same behaviour, so a message typed while the executor works reaches it at its next step instead of after the turn. One rule for every source: user message, concierge steer, child handback. A system-sourced message into an idle session starts a turn (the handback turn, fifty-turn backstop kept); a user message always does. Deleted with it: the client-side `queued` deque in `app.rs`, `send_steered` in the server, and the Jobs overlay's steer prompt, since typing into a live child's session is already a steer. Depends on row 1. | | todo |
| 3 | Handbacks reach a present parent the same way. A child finishing under a `:code` session appends its summary and footprint to the parent as today; with row 2 the parent reads it at its next step if live, or on a system-started turn if idle. Decide here whether a system-started turn in a session the user is sitting in should be visible as such (a dim note) so a reply that arrives unprompted is not mistaken for one to the last typed line. Depends on row 2. | | todo |
| 4 | Compaction as an event (DESIGN 4.4). Schema first, its own commit: `SessionCompacted` at `SessionEvent` field 10 with session id, `through_seq`, summary text, prompt version, and model. Then: `context_window` on `RoleConfig` (required for the executor, optional elsewhere; a role without one never compacts); the trigger reads the last step's reported prompt tokens against a configurable fraction of the window, default to be set from the first real firing; the summary is written by the session's own model under a versioned prompt with a `compaction` span label, validated before append, and the turn continues uncompacted if it does not parse; user messages are copied verbatim and the last few turns stay whole; the transcript builder in `session.rs` starts from the latest compaction and shows the summary as the first message after the system prompt. `:compact` in the TUI appends the same event through a new additive request. Tests: replay proves a compacted session builds the same transcript from the log alone; a fork before and after the event inherits the right prefix; a summary that fails validation leaves the log untouched. `arcd memory-replay` already runs a prompt against real history; the compaction prompt gets the same treatment before it ships. | | todo |
| 5 | Tool results on screen. A tool renders as one dim line, name, arguments, and outcome (`ui.rs` `Block::Tool`); an interactive coding session needs the bash tail and the edit result. Check first whether the stream's tool-result event carries the text; if not, add it additively. Render collapsed by default, opened with the same gesture that opens a thought (`Ctrl-o`), capped by the same output cap the tool already applies. Show the frame in the report. | | todo |
| 6 | The door follows the directory. A local `arc` started with its working directory under a configured project's root, canonical longest prefix, opens the pending `:code` door for that project, frontend-only until the first message, exactly the flow `C` and `:code` use; anywhere else it opens the concierge. `ProjectList` gains `root` (additive) so the client can match; the daemon keeps resolving grants. A remote client has no meaningful directory and opens the concierge; `:code` still works there. `Ctrl-n` from a code session opens a concierge conversation, as today. The picker's default filter scopes to the open project when one is open. **Done: the client canonicalizes its launch directory when the daemon is local, the project list is seeded on connect (`C` still fetches fresh), the longest component-wise root prefix opens the pending door, a key pressed before the seed arrives cancels it, and the session picker scopes to the open project until show-all. Remote clients never auto-open.** | | in review |
| 7 | Doctrine splits by door. The direct preamble gains three lines for a session that dispatches: dispatch then stop; briefs are self-contained; check a handback with your own tools before repeating it. `RUNNING_JOBS` stays the concierge's; the lines that exist because the concierge is blind (never describe unread code, report commits by id, compare against the footprint) are not copied. Watch the concierge's constant for lines that stop firing once development leaves it, and prune them. | | todo |
| 8 | Memory follows presence (DESIGN 5.4). The extractor mines sessions whose recorded creation source is the user, at either door, and titles the rest; the role gate in `consolidation.rs` becomes a source gate. `memory_write` is held wherever identity loads. No proto change; `source` is already in the projection. Replay-gate the change against the flood sessions the role gate was built on, so a `:code` transcript full of tool output does not bring the task-log records back. | | todo |
| 9 | Dispatch from a code session defaults to its own project. The tool's `project` argument accepts `"none"` for the bound caller's root, the convention `consult_expert` already uses, so a brief written from inside the arc session does not name arc. | | todo |

## Standing watches, carried from Phase 3.6

Not tasks until evidence arrives: whether the continue-vs-dispatch and cancelled-stays-stopped words keep holding; whether inline memory writes keep their stated scope; 8.7's week of dedup and review-queue numbers; 9.9's edit-tool friction; 9.2's grant-widening arm; whether reviews surface extraction misses, not just bad writes; whether records about one entity accumulate under different names; whether grounded summaries drift from what their sources say; whether a root's post-fork dead tail pollutes consolidation; whether a cancelled turn's orphaned child process ever bites; whether `a_cut_stream_appends_a_partial_reply` flakes again; whether deepseek keeps writing chatty comments once the preamble says not to. Added this phase: whether the executor's serving speed is what a session you sit in front of makes you feel, in which case the one-line model switch measured 2026-09-01 comes first.

## Not in this phase

- **Voice (Phase 4)** — waits on this phase's week, as it waited on the last.
- **A sandboxed worker, devices (Phase 5), embeddings** — unchanged.
